/**
 * Layout Components
 *
 * Shared structural components: Header, Footer, Sidebar, Navigation.
 * These wrap page content and provide consistent page chrome.
 *
 * Rules:
 * - Layout components must NOT contain business logic.
 * - Data fetching for navigation (e.g., restaurant name) must be done
 *   in a Server Component and passed as props.
 */
