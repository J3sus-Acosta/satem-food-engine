/**
 * Menu Components
 *
 * UI components for the digital menu: product cards, category filters,
 * product detail modal, and the menu grid.
 *
 * Rules:
 * - Prefer Server Components for static menu content (better performance).
 * - Use Client Components only for interactive parts (filters, modals).
 * - No business logic — receive data as props from Server Component parents.
 */
