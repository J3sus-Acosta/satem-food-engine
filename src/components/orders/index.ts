/**
 * Orders Components
 *
 * UI components for order management: order list, order detail card,
 * order status badge, and the kitchen display view.
 *
 * Rules:
 * - No business logic — receive order data as props.
 * - Status updates must go through the Orders Service via API Routes.
 * - Real-time updates should use polling or Server-Sent Events (future).
 */
