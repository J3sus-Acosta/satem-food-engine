/**
 * Chat Components
 *
 * UI components for the web chatbot: message bubbles, input field,
 * typing indicator, and the chat container shell.
 *
 * Rules:
 * - Chat components must be Client Components ('use client') as they
 *   require real-time interactivity and state.
 * - Business logic (message processing, AI calls) must live in services/chat/.
 * - These components only handle UI state and user interaction.
 */
