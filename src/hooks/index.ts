/**
 * Custom React Hooks
 *
 * Shared hooks used across multiple features.
 * Feature-specific hooks should be colocated with their feature.
 *
 * Rules:
 * - Hooks must start with `use` prefix.
 * - No business logic inside hooks — delegate to services.
 * - Hooks are always Client-side (they use React state/effects).
 */
