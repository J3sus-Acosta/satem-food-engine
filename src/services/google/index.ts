/**
 * Google Service
 *
 * Responsible for Google API integrations: Google Maps (location),
 * Google Business Profile, and any future Google Workspace integrations.
 *
 * Conventions:
 * - All functions must be pure and testable in isolation.
 * - API keys must be accessed via environment variables only.
 * - No HTTP logic — that belongs in API Route Handlers (app/api/).
 */
